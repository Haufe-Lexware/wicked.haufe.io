{
    "api": {
        "upstream_url": "http://mockbin.com/",
        "name": "orders",
        "request_path": "/orders"
    },
    "plugins": [
        {
            "name": "correlation-id",
            "config": {
                "header_name": "Correlation-Id",
                "generator": "uuid",
                "echo_downstream": false
            }
        }
    ]
}
